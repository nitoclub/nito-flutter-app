import 'package:flutter/material.dart';

class NitoScaffold extends Scaffold {
  const NitoScaffold({
    super.key,
    PreferredSizeWidget? appBar,
    Widget? body,
  }) : super(
          appBar: appBar,
          body: body,
        );
}
