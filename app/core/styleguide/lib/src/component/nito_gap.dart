import 'package:flutter/widgets.dart';
import 'package:nito_styleguide/nito_styleguide.dart';

/// スペース確保のためのウィジェット
class NitoGap extends SizedBox {
  const NitoGap.widthX025({super.key}) : super(width: Space.x025);

  const NitoGap.widthX05({super.key}) : super(width: Space.x05);

  const NitoGap.widthX1({super.key}) : super(width: Space.x1);

  const NitoGap.widthX2({super.key}) : super(width: Space.x2);

  const NitoGap.widthX3({super.key}) : super(width: Space.x3);

  const NitoGap.widthX4({super.key}) : super(width: Space.x4);

  const NitoGap.widthX5({super.key}) : super(width: Space.x5);

  const NitoGap.heightX025({super.key}) : super(height: Space.x025);

  const NitoGap.heightX05({super.key}) : super(height: Space.x05);

  const NitoGap.heightX1({super.key}) : super(height: Space.x1);

  const NitoGap.heightX2({super.key}) : super(height: Space.x2);

  const NitoGap.heightX3({super.key}) : super(height: Space.x3);

  const NitoGap.heightX4({super.key}) : super(height: Space.x4);

  const NitoGap.heightX5({super.key}) : super(height: Space.x5);
}
